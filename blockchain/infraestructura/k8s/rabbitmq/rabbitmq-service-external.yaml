apiVersion: v1
kind: Service
metadata:
  name: rabbitmq-external
  namespace: blockchain
spec:
  type: LoadBalancer
  selector:
    app: rabbitmq
  ports:
    - name: amqp
      port: 5672
      targetPort: 5672
