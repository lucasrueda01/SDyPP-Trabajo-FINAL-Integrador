config:
  clients:
    - url: http://loki:3100/loki/api/v1/push

  snippets:
    pipelineStages:
      - docker: {}
      - cri: {}

  positions:
    filename: /run/promtail/positions.yaml

daemonset:
  enabled: true

serviceMonitor:
  enabled: true

resources:
  requests:
    memory: 80Mi
    cpu: 50m
  limits:
    memory: 150Mi
    cpu: 150m
