apiVersion: v1
kind: Service
metadata:
  name: coordinator
  namespace: blockchain-apps
spec:
  type: ClusterIP
  selector:
    app: coordinator
  ports:
    - name: http
      port: 5000
      targetPort: 5000